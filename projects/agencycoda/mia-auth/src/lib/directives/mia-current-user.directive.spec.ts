import { MiaCurrentUserDirective } from './mia-current-user.directive';

describe('MiaCurrentUserDirective', () => {
  it('should create an instance', () => {
    const directive = new MiaCurrentUserDirective();
    expect(directive).toBeTruthy();
  });
});
