import { MiaPermissionOnlyDirective } from './mia-permission-only.directive';

describe('MiaPermissionOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new MiaPermissionOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
