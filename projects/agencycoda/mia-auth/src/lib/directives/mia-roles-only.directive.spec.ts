import { MiaRolesOnlyDirective } from './mia-roles-only.directive';

describe('MiaRolesOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new MiaRolesOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
